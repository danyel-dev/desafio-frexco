type listProductsProps = {
    products: Array<object>;
}

export default function ListProduct({ products }: listProductsProps) {
    console.log(products)
    
    return (
        <div>
            
        </div>
    );
}